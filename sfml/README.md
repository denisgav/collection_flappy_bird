To install library use command:
    sudo apt-get update
    sudo apt-get upgrade
    sudo apt-get install libsfml-dev build-essential

Installed version: (2.6.1+dfsg-2build2).

To compile, use command:
    make clean && make